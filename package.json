{
  "name": "demo",
  "private": true,
  "scripts": {
    "check": "cargo check --manifest-path Cargo.toml --lib --target wasm32-unknown-unknown",
    "build": "pnpm run check && wasm-pack build . --target web --out-dir ../generated/demo --out-name demo",
    "previews": "cargo run --release -- --headless ../scripts/previews/lilium.lua && cargo run --release -- --headless ../scripts/previews/flair.lua && cargo run --release -- --headless ../scripts/previews/inside-crescent-kick.lua && cargo run --release -- --headless ../scripts/previews/dancing-skeleton.lua && cargo run --release -- --headless ../scripts/previews/butterfly-kick.lua && cargo run --release -- --headless ../scripts/previews/butterfly-kick-uniform.lua && cargo run --release -- --headless ../scripts/previews/butterfly-kick-uniform-spacetime.lua && cargo run --release -- --headless ../scripts/previews/butterfly-kick-extended-importance-spacetime.lua && cargo run --release -- --headless ../scripts/previews/butterfly-kick-importance-spacetime.lua"
  },
  "devDependencies": {
    "wasm-pack": "^0.15.0"
  }
}
